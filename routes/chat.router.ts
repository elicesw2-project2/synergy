// import express, { Router } from 'express';

// const router: Router = express.Router();

// /* GET users listing. */
// router.get('/chatrooms/:workspace_idx', (req:Request, res:Response) => {console.log(req)});

// export default router;
